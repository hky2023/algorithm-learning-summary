# <span style="color:orange">单调队列体型 </span>

## 一：<span style="color:green">  单调队列结合线性dp

例如：有长度为n的数组，每一个位置i具有权值a[i],从位置i能够跨越到[i + 1, i + 5], 并且获得到达那个点的权值，试问到达位置n的最大可能值。

---
## 二： <span style="color:green"> 二维单调队列求矩形最大最小值
例如：给你一个 $n * m$大小的矩阵，让你求矩阵中$3 * 3$所有的子矩阵的最大值，最小值。
## 三：<span style="color:green"> 求数组中长度为[a, b]的最大最小值
往往是利用前缀和加上dp手法